// Tạo file trống hoặc Dummy widget để copy code List không lỗi
import 'package:flutter/material.dart';

class CoreCalendarSetting extends StatelessWidget {
  const CoreCalendarSetting({super.key});
  @override build(context) => const Scaffold(body: Center(child: Text(" Dummy Calendar Page")));
}